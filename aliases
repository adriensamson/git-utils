[alias]
    r = pull --rebase
    p = !git push -u origin $(git symbolic-ref HEAD | sed 's/refs\\/heads\\///')
    u = pull --ff-only
    s = status
    a = add -A :/
    ds = diff --staged
    nb = checkout -b
